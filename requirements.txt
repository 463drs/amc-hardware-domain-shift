# Python 3.12 (matches the default Kaggle notebook interpreter; developed against 3.12.13).
# Install into the activated conda env:  pip install -r requirements.txt

# visualisation related
matplotlib
ipython
ipykernel

# can affect results 

torch==2.10.0
numpy==2.0.2
h5py==3.16.0
scipy==1.16.3
pyyaml==6.0.2