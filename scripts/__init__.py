"""CLI entry points for the AMC project.

Each module here exposes a public function usable from a notebook plus a thin ``main()``
console wrapper, e.g.::

    from scripts.inspect_dataset import inspect_dataset
    from scripts.sanity_check import plot_constellations
"""
